namespace ParseRedisUrl {
    const redisDefaultPort = 6379;
    const sentinelDefaultPort = 26379;
    export function parseRedisUrl(url: string, sentinel: boolean = false, separator: string = ',; '): string[] {
        const result = <string[]>[];

        return result;
    }
}
